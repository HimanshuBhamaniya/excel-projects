## This Data cleaning Project is still under work. 
So far i have handled all the missing and nan values. 
Fixed the character case of values in Name field.
nan in salary field are converted to blank so that they won't interfere with calculations.
text values in numerical fields (Age, Salary) are converted to integers (eg. thirty-30).
dataset is converted to table for easy use but might get converted back. 
